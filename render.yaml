services:
  - type: web
    name: company-lead-scout
    env: python
    buildCommand: pip install -r requirements.txt ddgs
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
